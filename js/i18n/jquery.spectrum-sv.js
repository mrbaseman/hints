// Spectrum Colorpicker
// Swedish (sv) localization
// https://github.com/bgrins/spectrum

(function ( $ ) {

    var localization = $.spectrum.localization["sv"] = {
        cancelText: "Avbryt",
        chooseText: "V&auml;lj"
    };

    $.extend($.fn.spectrum.defaults, localization);

})( jQuery );
